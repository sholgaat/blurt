# This project now uses split env files per service:
# - .env.backend  (backend service only)
# - .env.bot      (bot service only)
#
# Copy templates:
#   cp .env.backend.example .env.backend
#   cp .env.bot.example .env.bot
